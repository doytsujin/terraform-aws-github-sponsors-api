{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug File",
      "type": "node",
      "request": "launch",
      "preLaunchTask": "Build for debugging",
      "program": "${file}",
      "outFiles": [
        "${workspaceFolder}/**/*.js"
      ],
      "smartStep": true
    }
  ]
}
